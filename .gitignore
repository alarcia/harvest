.DS_Store

# Internal working docs — agent context and task list, not for public
# consumption (personal workflow notes, decision history).
ROADMAP.md
CLAUDE.md

# Python
__pycache__/
*.py[cod]
.venv/

# Django
db.sqlite3
db.sqlite3-journal
# Working copies left by pull_prod_db.sh: the previous local database (.bak)
# and the in-flight download (.tmp). Real production data, never committed.
db.sqlite3.*
pull_prod_db.sh
staticfiles/

# Environment
.env
.env.example

# Test fixtures are REAL Amazon emails with personal data (names, addresses,
# order numbers) — kept local only, never committed. See tests/fixtures/README.md.
tests/fixtures/*.eml
